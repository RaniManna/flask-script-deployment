services:
  pos:
    image: ranimeta/meta:latest
    ports:
      - 3001:8080
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
    networks:
      - nginx-proxy-network
        
networks:
  nginx-proxy-network:
   external: true
